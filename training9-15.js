/*
var arr = [`/as/ad`, `/as/add`, `/asd/ad`, `/asd/add`, ];
var s = arr.indexOf(arr);
console.log(s);



var devices = ['/dev/console', '/dev/display'];
var index = devices.indexOf(dev);
console.log(index);


var devices = ['/dev/console', '/dev/display'];
function a (jj){
    var index = devices.indexOf(jj);
    console.log(index);
}
a( '/dev/display','/dev/console');
*/

var arr = [1,3,4,5]
var arr1 = arr.slice(1,2);
console.log(arr1);

arr.length=2;
console.log(arr);

